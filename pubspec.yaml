name: esc_pos_bluetooth
description: The library allows to print receipts using an ESC/POS thermal Bluetooth printer.
version: 0.4.1
publish_to: none
homepage: https://github.com/andrey-ushakov/esc_pos_bluetooth

environment: 
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.27.7
  esc_pos_utils: 
    git: 
      url: https://github.com/AbiMulLyA/esc_pos_utils.git
      ref: master
  flutter_bluetooth_basic:
    git: 
      url: https://github.com/AbiMulLyA/flutter_bluetooth_basic.git
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
